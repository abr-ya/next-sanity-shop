import { Badge } from "@/components/ui/badge";

const Home = () => (
  <main className="flex min-h-screen flex-col items-center p-24">
    <h1 className="text-xl mb-4">Hello, Next 14 + Tailwind CSS!</h1>
    <Badge variant="default">Badge</Badge>
  </main>
);

export default Home;
